# Pydantic AI: Type-Safe Agent with Governance

Type-safe structured output agent with TapPass governance on every LLM call.

## Setup

```bash
pip install tappass pydantic-ai
tappass quickstart
```

## Run

```bash
export TAPPASS_API_KEY=tp_...
python main.py
```
